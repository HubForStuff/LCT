# HANDOFF — July 16 2026 customer feedback

**Written:** 2026-07-17, mid-execution, at the user's request.
**Branch:** `feedback/2026-07-16-updates` (11 commits, ahead of `main`, never pushed)
**Working tree:** clean. **Nothing is merged.** `main` is untouched.

## Read these first, in order

1. `docs/superpowers/specs/2026-07-17-july-16-feedback-design.md` — the design + every decision the user made
2. `docs/superpowers/plans/2026-07-17-july-16-feedback-refinements.md` — the 9-task plan being executed
3. `.superpowers/sdd/progress.md` — the ledger: what's done, hazards, debt. **Trust this over memory.**
4. This file

Source of the feedback: `July 16 2026 updates.docx` (repo root). Events mockup: `mockup/5 - events_v11.htm`.

## Where execution stopped

Executing the refinements plan via `superpowers:subagent-driven-development`.

| Task | State |
|---|---|
| 1. Advisory restyle | ✅ complete, review clean (`ce58fe1`) |
| 2. Competition/Challenge detail | ✅ complete, review clean (`a9746a4`) |
| 3. Landing callouts | ✅ complete, review APPROVED after 2 fix rounds (`1484667`, `874ad01`, `12ea157`, `bb81427`) |
| 4. Network unpublish | ⚠️ **implemented (`bf0e092`) but NOT REVIEWED** |
| 5. Language switcher convergence | ⬜ not started |
| 6. Form benefit icons | ⬜ not started |
| 7. Eligibility form | ⬜ not started |
| 8. Programs list + detail | ⬜ not started |
| 9. CHECKLIST.md reconcile | ⬜ not started |
| — Events (H) | ⬜ **not planned yet** — needs its own plan |

## >>> RESUME HERE <<<

**Task 4's review was dispatched and interrupted before it ran. Task 4 is unreviewed. Do not mark it complete and do not move to Task 5 until it is reviewed.**

To resume: generate the package and dispatch a reviewer.

```bash
S=~/.claude/plugins/cache/claude-plugins-official/superpowers/6.1.1/skills/subagent-driven-development/scripts
"$S/review-package" bb81427 bf0e092   # or reuse .superpowers/sdd/review-bb81427..bf0e092.diff (already generated)
```

The reviewer needs `.superpowers/sdd/task-4-brief.md`, `.superpowers/sdd/task-4-report.md`, and that diff.

### What Task 4's review must scrutinise

**The index hazard is the whole ballgame.** `src/lib/homepage/links.ts` uses index-keyed maps and they are asymmetric:
- Desktop's Network was **last** → removing it shifts nothing.
- Mobile's Network was index **3 with Programs at 4** → a mechanical line-delete silently points Programs at the wrong target. No build error, no test failure.

The implementer renumbered Programs 4→3 (correct) and left a comment saying so. Verify every index map and every consumer, not just this one.

**A leftover mutation was found in the working tree during handoff** — `links.ts` had an uncommitted `3:` → `4:` edit (the broken state), left over from someone's verification and never reverted. I discarded it; the committed state is correct and the tree is clean. **Lesson for the resumer: check `git status` after every subagent.** A dirty tree carrying a mutation is how a silent wrong-link ships.

Also assess, from the implementer's own concerns:
1. They claim the brief was wrong twice: (a) there is no Network `footerColumns` group at all; (b) the brief **omitted** `src/content/interior-pages/locales/*.json` `navItems`, which carried Network and is index-aligned with `desktopMenuSections`. Missing it would have left a 404 nav item on **every interior page**. Verify both, especially that the alignment still holds.
2. Six tests hard-coded Network's presence. They updated them and **retargeted** some to `/programs/` rather than deleting coverage. Did intent survive, or is anything asserting less now?
3. The Network sub-pages test is **skipped, not deleted**.
4. `getStaticPageData("network")` now **throws**. They say nothing calls it.
5. The `published: false` flag lives in a schema **shared by all four static pages** — verify it can't break advisory/events/programs.
6. Reversibility is a requirement (this is temporary). They claim `git revert bf0e092` restores Network. Is that actually true given the test changes and renumbering?

**Independently verified already (you may rely on these):** `grep -rl "/network/" dist/` returns nothing; `dist/network` does not exist; build is clean.

## Hard-won facts — do not rediscover these

**THERE IS NO `tsconfig.json` IN THIS REPO.** `npx tsc --noEmit` finds no project, type-checks **nothing**, and exits 0. I cited it as evidence once; it was vacuous. There is **no typecheck in `npm run build` or `npm test`** — type errors surface only in an editor LSP. Never accept "tsc clean" as evidence. Prefer fixes that hold at runtime over type-level guards. (Worth telling the user: the repo has no CI typecheck at all. Separate from this work.)

**Baseline noise — never chase:**
- Test `interior footer WeChat popup is reachable on keyboard focus` — pre-existing flake, fails on a stashed clean tree.
- TypeScript errors in `src/lib/competitions/reader.ts` (~line 407) — pre-existing.
- Unused `challengePanelHtml` in `tests/interior-pages.test.mjs` (~line 1331, shifts as the file changes) — pre-existing since 2026-04-02.

**Codebase traps:**
- `.news-list-card` is shared by Advisory **and** News. Scope Advisory CSS to `.advisory-card-grid`.
- `/challenges/[slug]` and `/competitions/[slug]` share `CompetitionDetailPage.astro`. One template, two routes.
- Detail-page chrome copy lives in `detailCopyByCode` in `src/lib/competitions/reader.ts`, **not** the locale JSON. Diverges from AGENTS.md's i18n contract but is pre-existing and mirrored in `src/lib/programs/reader.ts`. **Task 8 should follow it.**
- `src/content/homepage/locales/br.json` is deliberately **diacritic-free**. Match it.

## For Task 6 specifically (form icons)

Task 3 built `src/components/site/icons.ts` for you and it took two rounds to get right:
- Add 12 entries to `ICON_PATHS`. **That is the whole requirement** — the Keystatic select derives from it via `Object.keys(ICON_PATHS)`, so icons auto-appear in the CMS. `ICON_LABELS` is an optional presentation override.
- `Icon.astro` **throws** on an unknown name. Deliberate and reviewer-upheld — a typo becomes a loud build failure rather than a shipped invisible icon. **Do not add a silent fallback.** This is the path Task 6 uses (form icons are hardcoded, not CMS-driven).
- `stroke-width` is hardcoded `1.5` with no prop. If any of the 12 icons need 1.2, that's a shared-component interface change mid-task.
- Nothing tests `icons.ts` directly. Cheap guard if wanted: iterate `Object.keys(ICON_PATHS)` and assert each renders.
- The icon→benefit mapping table is in the plan, Task 6, Step 3.

## Debt log — for the final whole-branch review to triage

- `src/lib/static-pages/schema.ts:97` — `summary` relaxed required→optional (Task 1). Necessary (Keystatic gates the build) but weakens validation for the whole shared staticPages collection.
- Detail-page chrome copy in `reader.ts` vs locale JSON — diverges from the stated i18n contract. Pre-existing.
- Callout `href` is per-locale editable in Keystatic, but `LocalizationClient` never re-applies `href` on language switch. Harmless today (all locales use identical hrefs); a br-only href edit would silently never apply. **Consequence of the content shape I specified — the user's call, not a subagent's.**
- Kept-but-unreferenced after Task 4: `src/lib/network/`, `src/components/network/`, `src/content/network/`, `src/content/speakers/`. Intentional for re-enable; dead-code tooling may flag them.

## Open questions for the user — NOT answered, do not guess

1. **Andre's landing copy** hasn't arrived. Task 3 shipped interim copy in the final content shape (user-approved), so the swap is content-only. Still needs replacing.
2. **"Book a Booth"** (Events): in the mockup with an `href="#"`, absent from the feedback. Spec defaults it to the contact modal. Confirm with Andre.
3. **"Submit your event" / "List Your Event"** (Events): in the mockup, absent from the feedback, no destination. **Out of scope** per the user — panel points at the contact modal, no organizer form. Confirm with Andre.
4. **Event application entry point**: feedback says Join opens the *detail* page, so the application form sits behind a second hop. Confirm the customer expects that.

## Deployment dependency (pre-existing, not introduced here)

`scripts/apps-script/Code.gs` is **not deployed** and `PUBLIC_PREREG_SCRIPT_URL` is **not set**. Task 7's eligibility form inherits this: it will be code-complete but **not live** until the user deploys. Do not claim the form submits successfully. Task 7 adds `formType` routing to `Code.gs` (`pre-registration` → `Submissions`, `eligibility` → `Eligibility`), so the redeploy covers both.

## Three forms, not one — do not conflate

The user pushed back on this specifically and was right:

| Form | Who | Entry | Status |
|---|---|---|---|
| Check Eligibility | company qualifying for a **program** | Programs detail | Task 7 |
| Event application | someone **attending an event** | Events detail | Events plan |
| Submit your event | an **organizer listing** an event | events list panel | **out of scope** |

Each in-scope form gets its **own** component, route, and column set. They share only the Apps Script endpoint via `formType`. Do not "simplify" by reusing one component across purposes.

## Process notes that earned their keep

- **The plan had real defects and subagents caught them.** Task 3's brief asserted no `/network/` links document-wide — unsatisfiable until Task 4. Task 4's brief invented a footer column that doesn't exist and missed `navItems` entirely. **When an implementer stops to ask, that's the process working.** Answer, fix the plan, move on.
- Reviews found things that mattered twice on Task 3: a test that couldn't fail for its stated reason, and a "fix" that relocated the bug instead of killing it. Don't skip the re-review after a fix.
- User's workflow (from memory): brainstorm → spec → plan → subagent exec. Squash-merge to `main`, delete branch, push to `origin/ash/lct`. **Author Ash, no Claude trailer.**

## After the refinements plan

Events (area H) still needs its own plan written from the spec + `mockup/5 - events_v11.htm`. It's a new content collection (21 events), list with filters/sort/month-grouping, detail page, and application form. The mockup **reuses the Competitions list's class names verbatim** (`ccard`, `filter-pill`, `badge-open`, `comp-grid`) — it's designed to reuse that machinery, not fork it. Then: final whole-branch review, then `superpowers:finishing-a-development-branch`.
