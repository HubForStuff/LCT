I want you to check current state of the website and do the following changes:
1. max width is not the same as the mockup in `mockup/1\ -\ LATAMCHINATECH_desktop\ v\ 1million.htm`
2. The internal pages should have the same menu as the main layout but with a black background and white font. The menu in both parts should have the same width as the page (content part)
3. The main page's inicial part "Accelerating inovation" should have a background video in loop. I've added a sample video in the public directory, but we might change this later, the is for testing purposes only, but the functionality must be there.
4. The footer has a bug triggered by the language menu causing the page to extend further. Change the language menu in the footer to display on top of the icon instead of the bottom.
5. Competitions and corporate challenges each should be linked to a different tab in the challenges page. Competitions link should display the first tab (Startup competitions) and the 
6. The card stack in the main page should be stacked together displaying only the title and only display the description once the user mouse hover the card (like a deck of cards). The last card in the bottom should always display fully (because there is nothing covering it).
7. the footer in the internal pages are also overflowing, it should stick to the bottom
8. News cards in the main page have title in the bottom but should be at the top of the card.
9. I need a full map of the site with all the existing and non-existing links (some links are based on the mockup and are not yet available) please draw it with draw.io (install a skill if necessary) with all page sections and use some text to demonstrate what is ready and what is not. Take a screenshot of each page and attach to the map


## Issues

1. Nav Menu should have white background in internal pages, and should have a drop menu with black color - white font. All pages nav menu should be consistent in functionality, but internal pages have top nav menu with white background black font and drop menu with black background, main page has black background white font and drop menu white bg black font
2. Drop down menu width should
2. the card deck should display the title, so make the visible part higher, and also the mouse hover effect should slide down the other cards to display the whole card contents. Fix the z-index of elemets to avoid overlays. The overall height of the section should be stable instead of always changing the whole height of the website. This is a good start
3. News cards style should be adjusted, follow the mockup style.
4. max width problem is resolved but it should not be always expanding, find a suitable max width
