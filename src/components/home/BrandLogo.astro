---
import SiteBrandLogo from "../site/BrandLogo.astro";

interface Props {
  alt: string;
  className?: string;
}

const { alt, className = "" } = Astro.props;
---

<SiteBrandLogo alt={alt} className={["home-logo", className].filter(Boolean).join(" ")} />
